spring:
  application:
    name: config-server
  profiles:
    active:
    - context
    - eureka
  cloud:
    config:
      server:
        git:
          uri: https://github.com/BarathArivazhagan/config-server-repository
      enabled: true
       
   