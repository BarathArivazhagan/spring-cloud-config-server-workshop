spring:
  application:    name: config-client
  cloud:
    config:
      discovery:        service-id: config-server
        enabled: true 
      name: config-client-sample  profiles:    active:    - eureka
       